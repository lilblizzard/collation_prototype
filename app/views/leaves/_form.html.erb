<%= form_with(model: @leaf, url: quire_leaf_path(@leaf), local: true) do |form| %>
  <% if leaf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manuscript.errors.count, "error") %> prohibited this manuscript from being saved:</h2>

      <ul>
        <% manuscript.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Single?" %>
    <%= form.check_box :single %>
  </div>

  <div class="field">
    <%= form.label "Mode?" %>
    <%= form.select :mode, Leaf::MODES, {}, { class: "form-control" } %>
  </div>

  <div class="actions pt-2">
    <%= form.submit class: "form-control btn btn-lg btn-primary btn-block" %>
  </div>
<% end %>
